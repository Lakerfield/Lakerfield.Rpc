<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>Lakerfield.Rpc</RootNamespace>
    <Authors>Michael Lakerveld</Authors>
    <Company>Lakerfield B.V.</Company>
    <Product>Lakerfield.Rpc</Product>
    <Copyright>Lakerfield B.V.</Copyright>
    <AssemblyTitle>Lakerfield.Rpc.WebSocketServer</AssemblyTitle>
    <Product>Lakerfield.Rpc.WebSocketServer</Product>
    <PackageId>Lakerfield.Rpc.WebSocketServer</PackageId>
    <Description>The server library for a .Net RPC libary over a websocket using BSON</Description>
    <PackageDescription>A server library for a .Net RPC libary over a websocket using the MongoDB.Bson library</PackageDescription>
    <PackageTags>$(PackageTags);server;websocket</PackageTags>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Lakerfield.Rpc\Lakerfield.Rpc.csproj" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.Reactive" />
  </ItemGroup>

</Project>
